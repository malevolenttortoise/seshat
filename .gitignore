# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg
.venv/
venv/
env/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# Editors
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Seshat runtime
data/
files/
*.db
*.db-shm
*.db-wal
settings.json
auth_secret
debug.log

# Real docker-compose with secrets — only the .example version is committed
docker-compose.yml
docker-compose.override.yml

# Frontend (added in Phase 3)
node_modules/
frontend/dist/

# Local scratch
scratch/
.scratch/
.tmp/
*.tsbuildinfo

# Understand Anything knowledge graph — local-only codebase analysis artifacts
.understand-anything/

# Local dev stack — Docker Compose sandbox for testing destructive
# features (Phase 5b file-swap, etc.) against real Calibre + CWA + ABS
# on Mark's Bazzite PC. Entire directory (configs + scripts + data) is
# local-only. See dev-stack/README.md if/when the folder exists locally.
dev-stack/
